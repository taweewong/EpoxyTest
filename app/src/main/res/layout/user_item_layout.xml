<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/user_item_container"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/trans_black_round_bg"
    android:paddingTop="20dp"
    android:paddingBottom="20dp"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:layout_marginTop="4dp"
    android:layout_marginBottom="4dp"
    android:layout_marginStart="4dp"
    android:layout_marginEnd="8dp">

    <TextView
        android:id="@+id/user_item_display_name_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Display Name"
        android:textSize="20sp"
        android:textColor="@android:color/white"
        android:fontFamily="@font/valera_round_regular" />

    <TextView
        android:id="@+id/user_item_name_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="first last"
        android:textSize="16sp"
        android:textStyle=""
        android:textColor="@android:color/white"
        android:fontFamily="@font/valera_round_regular" />

</LinearLayout>